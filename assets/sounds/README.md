# 默认提示音目录

把默认专注结束提示音放在这个目录中。

程序会按下面的文件名顺序查找并播放：

- `focus-default.wav`
- `focus-default.mp3`
- `focus-default.ogg`
- `focus-default.m4a`

如果你设置了“修改提示音”，则会优先播放你手动选择的文件。

如果这个目录里没有可用音频文件，程序会退回到内置蜂鸣音。
